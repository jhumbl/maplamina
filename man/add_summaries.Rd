% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components_summaries.R
\name{add_summaries}
\alias{add_summaries}
\title{Add summaries to a layer}
\usage{
add_summaries(
  map,
  ...,
  id = NULL,
  bind = NULL,
  position = NULL,
  layer_id = NULL
)
}
\arguments{
\item{map}{A maplamina widget created by \code{\link[=maplamina]{maplamina()}}.}

\item{...}{One or more \verb{summary_*()} objects (or a single list of them).}

\item{id}{Optional id used as a shorthand bind id when \code{bind} is omitted.}

\item{bind}{Bind group id for shared UI control.}

\item{position}{Optional UI position hint (applied to the control group).}

\item{layer_id}{Target layer id (defaults to the most recently added layer).}
}
\value{
The modified map widget.
}
\description{
Registers one or more summary rows that update with filtering. When multiple
layers share the same \code{bind}, a single summaries card can be created.
}
\examples{
d <- data.frame(
  lon   = runif(1000, -60, 60),
  lat   = runif(1000, -60, 60),
  value = runif(1000, 1, 10)
)
maplamina() |>
  add_circles(d) |>
  add_filters(filter_range(~value), bind = "filters") |>
  add_summaries(summary_mean(~value, label = "Avg value"), bind = "summaries") |>
  add_panel(sections = sections(section("filters"), section("summaries")))
}
