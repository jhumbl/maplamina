% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer_icon.R
\name{add_icons}
\alias{add_icons}
\title{Add an icon layer}
\usage{
add_icons(
  map,
  data = NULL,
  lon = NULL,
  lat = NULL,
  icon = "marker",
  size = 18,
  color = "#3388ff",
  opacity = 1,
  tooltip = NULL,
  popup = NULL,
  id = NULL,
  group = NULL,
  pickable = TRUE,
  size_units = c("pixels", "meters", "common"),
  size_min_pixels = NULL,
  size_max_pixels = 64,
  icon_anchor = NULL,
  mask = TRUE,
  occlude = FALSE
)
}
\arguments{
\item{map}{A maplamina widget created by \code{\link[=maplamina]{maplamina()}}.}

\item{data}{Data for this layer. If \code{NULL}, uses the default \code{data} supplied to \code{\link[=maplamina]{maplamina()}}.}

\item{lon, lat}{Longitude/latitude aesthetics (formula or scalar). Required for non-\code{sf} data.}

\item{icon}{Icon id string (e.g. \code{"marker"}).}

\item{size, color, opacity}{Icon size and color/opacity (can be formulas).}

\item{tooltip, popup}{Optional \code{\link[=tmpl]{tmpl()}} objects (or \code{NULL}) for hover/click content.}

\item{id, group}{Optional layer id and group name.}

\item{pickable}{Logical; whether features can be picked (tooltip/popup).}

\item{size_units}{Units for \code{size}; one of \code{"pixels"}, \code{"meters"}, or \code{"common"}.}

\item{size_min_pixels, size_max_pixels}{Optional clamp in pixels when using meter/common units.}

\item{icon_anchor}{Optional anchor (frontend-specific).}

\item{mask}{Logical; whether to mask the icon.}

\item{occlude}{Logical; whether icons occlude each other.}
}
\value{
The modified map widget.
}
\description{
Adds a point icon layer. Icons are referenced by id (resolved in the frontend icon set).
}
\examples{
q <- datasets::quakes
maplamina(q) |>
  add_icons(icon = "marker", size = 20)
}
