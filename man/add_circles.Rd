% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer_circle.R
\name{add_circles}
\alias{add_circles}
\title{Add a circle layer}
\usage{
add_circles(
  map,
  data = NULL,
  lon = NULL,
  lat = NULL,
  color = "darkblue",
  opacity = 1,
  width = 1,
  fill_color = "dodgerblue",
  fill_opacity = 0.8,
  radius = 6,
  stroke = TRUE,
  tooltip = NULL,
  popup = NULL,
  id = NULL,
  group = NULL,
  pickable = TRUE,
  radius_units = c("pixels", "meters", "common"),
  radius_min_pixels = NULL,
  radius_max_pixels = NULL,
  width_units = c("pixels", "meters", "common"),
  width_min_pixels = NULL,
  width_max_pixels = NULL
)
}
\arguments{
\item{map}{A maplamina widget created by \code{\link[=maplamina]{maplamina()}}.}

\item{data}{Data for this layer. If \code{NULL}, uses the default \code{data} supplied to \code{\link[=maplamina]{maplamina()}}.}

\item{lon, lat}{Longitude/latitude aesthetics (formula or scalar). Required for non-\code{sf} data.}

\item{color, opacity, width}{Stroke color/opacity/width.}

\item{fill_color, fill_opacity}{Fill color/opacity. \code{fill_color} can be a single color,
a vector of colors, or a color scale spec such as \code{\link[=color_quantile]{color_quantile()}}.}

\item{radius}{Circle radius (numeric or formula).}

\item{stroke}{Logical; draw circle stroke.}

\item{tooltip, popup}{Optional \code{\link[=tmpl]{tmpl()}} objects (or \code{NULL}) for hover/click content.}

\item{id, group}{Optional layer id and group name.}

\item{pickable}{Logical; whether features can be picked (tooltip/popup).}

\item{radius_units}{Units for \code{radius}; one of \code{"pixels"}, \code{"meters"}, or \code{"common"}.}

\item{radius_min_pixels, radius_max_pixels}{Optional clamp in pixels when using meter/common units.}

\item{width_units}{Units for stroke \code{width}; one of \code{"pixels"}, \code{"meters"}, or \code{"common"}.}

\item{width_min_pixels, width_max_pixels}{Optional clamp in pixels when using meter/common units.}
}
\value{
The modified map widget.
}
\description{
Adds a GPU-rendered circle layer (points). Use formulas (e.g. \code{~mag * 2}) to
map columns to aesthetics.
}
\examples{
d <- data.frame(
  lon   = runif(1000, -60, 60),
  lat   = runif(1000, -60, 60),
  value = runif(1000, 1, 10)
)
maplamina() |>
  add_circles(d, radius = ~value, fill_color = "dodgerblue")
}
