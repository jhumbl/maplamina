% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components_views.R
\name{add_views}
\alias{add_views}
\title{Add views to a layer}
\usage{
add_views(
  map,
  ...,
  id = NULL,
  bind = NULL,
  position = NULL,
  layer_id = NULL,
  duration = 750,
  easing = c("smoothstep", "linear", "easein", "easeout", "easeinout", "easeInOutCubic")
)
}
\arguments{
\item{map}{A maplamina widget created by \code{\link[=maplamina]{maplamina()}}.}

\item{...}{One or more \code{\link[=view]{view()}} objects (or a single list of them).}

\item{id}{Optional component id (also used as the default bind id).}

\item{bind}{Bind group id for shared UI control. If omitted, defaults to \code{id} (or an auto id).}

\item{position}{Optional UI position hint (applied to the control group).}

\item{layer_id}{Target layer id (defaults to the most recently added layer).}

\item{duration}{Animation duration (ms) for switching views.}

\item{easing}{Easing function name for switching views.}
}
\value{
The modified map widget.
}
\description{
Registers a per-layer views component. When multiple layers share the same \code{bind},
a single selector control is created.
}
\examples{
q <- datasets::quakes
maplamina(q) |>
  add_circles(lon = ~long, lat = ~lat, radius = 5) |>
  add_views(
    view("magnitude", radius = ~mag * 3),
    view("faint", fill_opacity = 0.2),
    bind = "views"
  )
}
