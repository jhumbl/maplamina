% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer_polygon.R
\name{add_polygons}
\alias{add_polygons}
\title{Add a polygon layer}
\usage{
add_polygons(
  map,
  data = NULL,
  color = "darkblue",
  opacity = 1,
  width = 1,
  fill_color = "dodgerblue",
  fill_opacity = 0.6,
  stroke = TRUE,
  elevation = NULL,
  elevation_scale = 1,
  tooltip = NULL,
  popup = NULL,
  id = NULL,
  group = NULL,
  pickable = TRUE,
  width_units = c("pixels", "meters", "common"),
  width_min_pixels = NULL,
  width_max_pixels = NULL
)
}
\arguments{
\item{map}{A maplamina widget created by \code{\link[=maplamina]{maplamina()}}.}

\item{data}{Data for this layer. Typically an \code{sf} object with POLYGON/MULTIPOLYGON geometry.}

\item{color, opacity, width}{Stroke color/opacity/width.}

\item{fill_color, fill_opacity}{Fill color/opacity. \code{fill_color} can be a single color,
a vector of colors, or a color scale spec such as \code{\link[=color_quantile]{color_quantile()}}.}

\item{stroke}{Logical; draw polygon stroke.}

\item{elevation, elevation_scale}{Optional extrusion height (numeric or formula) and scale.}

\item{tooltip, popup}{Optional \code{\link[=tmpl]{tmpl()}} objects (or \code{NULL}) for hover/click content.}

\item{id, group}{Optional layer id and group name.}

\item{pickable}{Logical; whether features can be picked (tooltip/popup).}

\item{width_units}{Units for stroke \code{width}; one of \code{"pixels"}, \code{"meters"}, or \code{"common"}.}

\item{width_min_pixels, width_max_pixels}{Optional clamp in pixels when using meter/common units.}
}
\value{
The modified map widget.
}
\description{
Adds a polygon layer (fill + optional stroke). For data-driven fills, use a
color scale spec such as \code{\link[=color_quantile]{color_quantile()}} or \code{\link[=color_factor]{color_factor()}}.
}
\examples{
if (requireNamespace("sf", quietly = TRUE)) {
  nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
  maplamina(nc) |>
    add_polygons(fill_color = color_quantile(~BIR74), stroke = FALSE)
}
}
