% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components_filters.R
\name{add_filters}
\alias{add_filters}
\title{Add filters to a layer}
\usage{
add_filters(map, ..., id = NULL, bind = NULL, position = NULL, layer_id = NULL)
}
\arguments{
\item{map}{A maplamina widget created by \code{\link[=maplamina]{maplamina()}}.}

\item{...}{One or more \code{\link[=filter_range]{filter_range()}} / \code{\link[=filter_select]{filter_select()}} objects (or a single list of them).}

\item{id}{Optional id used as a shorthand bind id when \code{bind} is omitted.}

\item{bind}{Bind group id for shared UI control.}

\item{position}{Optional UI position hint (applied to the control group).}

\item{layer_id}{Target layer id (defaults to the most recently added layer).}
}
\value{
The modified map widget.
}
\description{
Registers one or more filters for a layer. When multiple layers share the same \code{bind},
controls merge across layers by (bind, label, type).
}
\examples{
q <- datasets::quakes
maplamina(q) |>
  add_circles(lon = ~long, lat = ~lat) |>
  add_filters(
    filter_range(~mag, default = c(4, 6)),
    bind = "filters"
  )
}
